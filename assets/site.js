(()=>{
 const slides=[...document.querySelectorAll('[data-slide]')],copies=[...document.querySelectorAll('[data-copy]')],dots=[...document.querySelectorAll('[data-dot]')],number=document.querySelector('[data-slide-number]');let active=0,timer;
 function show(index){if(!slides.length)return;active=(index+slides.length)%slides.length;slides.forEach((el,i)=>el.classList.toggle('active',i===active));copies.forEach((el,i)=>el.classList.toggle('active',i===active));dots.forEach((el,i)=>el.classList.toggle('active',i===active));if(number)number.textContent=String(active+1).padStart(2,'0');}
 function restart(){clearInterval(timer);timer=setInterval(()=>show(active+1),5600)}dots.forEach((el,i)=>el.addEventListener('click',()=>{show(i);restart()}));if(slides.length>1)restart();
 const toggle=document.querySelector('[data-menu-toggle]'),nav=document.querySelector('.main-nav');if(toggle&&nav){const closeMenu=()=>{nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')};toggle.addEventListener('click',()=>{nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(nav.classList.contains('open')))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()});document.addEventListener('click',e=>{if(nav.classList.contains('open')&&!nav.contains(e.target)&&!toggle.contains(e.target))closeMenu()});}
})();
